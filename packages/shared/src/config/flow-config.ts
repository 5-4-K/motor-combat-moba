export const FLOW_CONFIG = {
  carSelectSeconds: 60,
  /** How long the "cars locked in" grid holds before the match countdown starts. */
  revealSeconds: 10,
  countdownSeconds: 3,
  nameMin: 1,
  nameMax: 16,
} as const;
