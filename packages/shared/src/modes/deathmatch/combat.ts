// GENERATED then hand-maintained: seeded verbatim from today's live config by
// scripts/seed-mode-folders.mjs (deleted after use), then committed as ordinary source.

import type { CombatConfig } from "../../config/combat-config.js";

export const DEATHMATCH_COMBAT = {
  hpPerRating: 10,
  attackBaseline: 50,
  damagePerAttack: 0.01,
} as const satisfies CombatConfig;
