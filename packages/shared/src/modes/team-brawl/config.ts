import type { ModeOverrides } from "../merge.js";

/**
 * Team brawl's differences from the base (`modes/base.ts`). Empty today; Team brawl has its own
 * folder so it can diverge from Brawl without touching it.
 */
export const TEAM_OVERRIDES: ModeOverrides = {};
