// GENERATED then hand-maintained: seeded verbatim from today's live config by
// scripts/seed-mode-folders.mjs (deleted after use), then committed as ordinary source.

import type { SpikeConfig } from "../../config/spike-config.js";

export const CONQUER_SPIKE = {
  damage: 80,
  depth: 20,
  triggerSpeed: 25,
  retriggerMs: 750,
  shoverCreditMs: 4000,
  contactPad: 2,
} as const satisfies SpikeConfig;
