// GENERATED then hand-maintained: seeded verbatim from today's live config by
// scripts/seed-mode-folders.mjs (deleted after use), then committed as ordinary source.

import type { TurretConfig } from "../../config/turret-config.js";

export const CONQUER_TURRET = {
  turnRateDegPerSec: 540,
  defaultOffset: 25,
  maxSwingDeg: 360,
} as const satisfies TurretConfig;
